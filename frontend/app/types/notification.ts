export interface NotificationSettings {
  push_enabled: boolean;
  email_enabled: boolean;
  daily_suggestion: boolean;
  weekly_summary: boolean;
  monthly_summary: boolean;
  streak_reminder: boolean;
}
