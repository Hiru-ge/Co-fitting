# TODO — ベータ版リリースロードマップ

> Phase 1 の実装はすべて完了。リファクタリング → ベータ版準備 → 公開の流れで **3月1週目（3/6頃）** にベータ版を公開する。

---

## 🚀 ベータ版ロードマップ（2/28 → 3/6）

### 2/28（土）AM: リファクタリング

- [x] Issue #205（バックエンド品質改善 7件）を完了させる
- [x] Issue #206（フロントエンド品質改善 8件）を完了させる

### 2/28（土）PM: UX改善

- [x] 動作確認しながらUXの摩擦を洗い出し・修正する
  - モバイルでの操作感、画面遷移、ローディング状態、エラー表示など
- [x] Issue #222（UX改善 5件）を完了させる
  - マップ表示時の表示半径初期値を狭くする
  - 「脱却モード」「興味タグ一致」バッジの視認性改善（バッジ透過度低減・バッジ色変更）
  - パーソナライズ提案ロジック `selectPersonalizedPlaces` の見直し（興味タグ2枠＋完全ランダム1枠）
  - リロード後に3件以上の提案が表示されるバグ修正
  - ライトモードでの見栄え修正（背景色・コントラスト改善）

### 3/1（土）~3/2（月）: インフラ準備・ベータ版セットアップ

- [x] **Issue #231: 無料PaaSへのデプロイ**
  - Frontend: **Cloudflare Pages**（帯域無制限、`roamble.pages.dev`）
  - Backend: **Render Free**（512MB RAM、15分スリープ → UptimeRobotで回避）
  - DB: **TiDB Cloud Starter**（MySQL 8.x互換、5GB、接続文字列変更のみ。**Spending Limit $0 に設定すること**）
  - Redis: **Upstash Redis**（500K commands/月）
  - 本番環境用の環境変数（`DATABASE_URL`, `REDIS_URL`, `JWT_SECRET`, `GOOGLE_CLIENT_ID` 等）を設定
  - CORS設定を `roamble.pages.dev` に対応させる
- [x] **Issue #232: 合言葉アクセス制限の実装**
  - フロントエンドのみで実装。初回アクセス時に合言葉入力画面を表示し、正しければlocalStorageにフラグ保存
  - 合言葉はベータテスター募集時に通知する
- [x] **Issue #233: プライバシーポリシーの作成・設置**
  - Google OAuthを使用しているため必須。位置情報の取り扱い・Place IDのみサーバー保存などを明記
  - アプリ内のフッターまたは設定画面にリンクを設置
- [x] **Issue #234: お問い合わせフォーム（Google Form）の設置**
  - Google Formでバグ報告・フィードバック用のフォームを作成
  - アプリ内の設定画面にリンクを設置
- [x] **Issue #235: Google Analytics（GA4）の設定**
  - フロントエンドにGA4のトラッキングコードを追加
  - 主要イベント（提案生成、訪問記録、バッジ獲得）のカスタムイベントを設定

### 3/3（火）- 3/6（金）: 本番動作確認・UX改善

- [ ] **本番環境での動作確認**
  - デプロイ後に一通り操作確認（OAuth、提案生成、訪問記録、XP付与、バッジ）
  - スリープからの復帰時間が許容範囲か確認
  - モバイルでの表示・操作感を確認
- [ ] **本番環境でのUX改善**
  - [ ] Issue #249: ローディングパフォーマンス改善（実機確認）
  - [x] Issue #250: モバイルレスポンシブ対応の実機確認・修正(とりあえずは完了。このタスクは曖昧すぎるので、具体的な問題が見つかったら個別Issueに切り出す)
  - [x] Issue #252: 位置情報変化による自動再提案機能の排除
    - 「現在地がある程度変わったら自動で再提案」の仕様はユーザーの混乱を招くため削除する
    - 位置情報変化の監視処理・再提案トリガーをフロントエンドから取り除く
  - [x] Issue #253: 提案カードの情報レイアウトを2段化する
    - 「地図で開く」ボタン追加によりカード下部の文字が改行する問題を解消
    - 施設名・ジャンル・距離・ボタン群を最初から2段レイアウトで設計し直す
    - 結果：要は一行当たりの情報量が多いのが問題だったので、店名の下に(Google Mapsで開く)ボタンを配置する方法で解消した。本来のHowとは違うが、このほうが良い
  - [ ] Issue #254: PWA以外の環境でも適切な表示にする
    - PWA（ホーム画面追加）でのみ見た目が整う状態を解消
    - ブラウザ単体でのアクセス時もPWAと同等のレイアウト・表示品質を担保する
  - [x] Issue #255: 脱却モードの判定仕様を変更する
    - 現行: 熟練度Lv.1のジャンルへの訪問 → 問題点: 最初の訪問が必ず脱却になる
    - **新仕様: 「興味タグ外 かつ 熟練度Lv.5以下」のジャンルへの訪問を脱却扱い**
    - ジャンル熟練度の最大はLv.20。Lv.6以上になれば興味タグ外でも通常扱い
    - 興味タグ内のジャンルへの訪問は脱却扱いしない
    - バックエンドのXP付与ロジック・フロントの脱却表示判定を両方変更する
    - 仕様書(docs/requirements.md)の該当箇所も更新する
  - [x] Issue #256: 訪問履歴マップの中心位置をユーザーの現在地に修正する
    - **背景**: 訪問履歴マップが常に渋谷駅（デフォルト中心）を表示してしまう
    - 🔴 RED: マップ表示時に、デフォルト中心がユーザーの現在地（またはvisits配列の平均座標）を基準に中心設定されることを確認するテストを書く
    - 🟢 GREEN: `frontend/app/routes/visits-map.tsx` でマップ初期化時に、`geolocation API` または `visits` の座標データから適切な中心座標を計算して Leaflet に渡す
    - 🔵 REFACTOR: マップ中心計算ロジックをユーティリティ関数に切り出す
  - [x] Issue #257: 訪問ボタンは施設周辺でのみ押せるようにする
    - **背景**: 現在は距離に関係なく「訪問」ボタンを押せる。ユーザーが実際に施設周辺に到着してからのみ押可能にしたい
    - **仕様案**: 現在地と施設の距離が200m以内の場合のみボタンを有効化（距離が遠い場合はボタングレーアウト、ツールチップで「この場所に到着してから記録できます」と表示）ただし、ENVIRONMENT=developmentのときは常に有効化（開発・テストの利便性のため）
    - 🔴 RED: 現在地が施設から200m以内のとき訪問ボタンが有効で、100mより遠いとき無効になることを確認するテストを書く(環境変数に注意)
    - 🟢 GREEN: `geolocation API` で現在地を取得し、`haversine` など距離計算アルゴリズムでPlace IDの施設との距離を算出。ボタンの`disabled`属性と表示メッセージを動的に変更する
    - 🔵 REFACTOR: 距離計算ロジックをユーティリティ関数に切り出す
  - [x] Issue #258: ホーム画面にチュートリアルツアーモーダルを追加する
    - **背景**: ユーザーの迷い・操作困難感を低減するため、初めてホーム画面に到着したとき「提案の使い方」「訪問記録の方法」「XP・バッジの仕組み」を説明するモーダルを表示する
    - **フラグ設計**:
      - 既存の興味タグオンボーディングは `onboarding_skipped`（`ONBOARDING_SKIPPED_KEY`）で制御されており、その仕組みとは**完全に独立**
      - 本ツアー用に `HOME_TOUR_SEEN_KEY = "home_tour_seen"` を `constants.ts` に別途追加
      - 興味タグをスキップしてホームに来た場合でも、ツアーは独立して表示される
      - localStorage削除時は再表示されるが、ベータ版では仕様として許容（ツアーを見ること自体のコストが低いため）
    - **表示条件**: `home.tsx` コンポーネント内で `localStorage.getItem(HOME_TOUR_SEEN_KEY)` が `null` のとき表示
    - **UI仕様**:
      - `onboarding.tsx` と統一感のあるデザイン（同一のカラー・ボタンスタイル）
      - 説明している箇所にスポットライトが当たる感じにする（背景を暗くして、説明対象のUI要素を明るくする）
      - ステップ送りボタン（「次へ」）とスキップボタンを `onboarding.tsx` のボタンスタイルと揃える
      - ステップ例: ①「近くの場所が提案されます(カードをスワイプで提案を移動・リロードで提案カードの引き直し)」→ ②「気に入った場所に行ったら訪問を記録しましょう(半径100m以内で押下可能)」→ ③「訪問するたびにXPとバッジが貯まります」→ 「はじめる」
      - モーダルを閉じる（スキップ or 完了）と `localStorage.setItem(HOME_TOUR_SEEN_KEY, "true")` を記録
    - **実装場所**: `home.tsx` コンポーネント内でモーダルの条件付き描画（`root.tsx` には置かない）
    - 🔴 RED: `localStorage` に `home_tour_seen` がないとき `HomeTourModal` が表示され、フラグがあるときは表示されないことをテストで確認する。ステップ送り・スキップ時にフラグが書き込まれることも確認する
    - 🟢 GREEN: `frontend/app/components/HomeTourModal.tsx` を作成し、`home.tsx` の `useState` + `useEffect` で初回判定・条件付き描画を実装。`HOME_TOUR_SEEN_KEY` を `constants.ts` に追加する
    - 🔵 REFACTOR: ステップコンテンツ（テキスト）を `constants.ts` または別ファイルに切り出し、`onboarding.tsx` との共通スタイルをTailwindクラスで整理する

### 3/7（土）: ベータ版公開

- [ ] **説明動画の撮り直し**（あなたの作業）
  - Roambleの使い方・コンセプトを短くまとめた動画を撮影
  - SNS（X, TikTok等）でベータ版参加を再告知
- [ ] **ベータ版公開**
  - SNSで公開アナウンス。合言葉をDMまたはフォーム回答者に通知
  - LP（Notion）にベータ版URLを追記
- [ ] **ベータ版フォーム締め切り**（公開1週間後 = 3/14頃）

### 3/8〜: Optional（ひと段落後）

- [ ] LPをNotionから独自ページに移行（Cloudflare Pagesで同一ドメインに設置可能）
- [ ] Product Hunt "Upcoming" ページの活用

---

## 🔮 Phase 2 計画（MVP公開後・ドッグフーディングFB次第）

> 仕様見直し（2026-02-27議論結果）を受けたPhase 2タスク群。
> MVP公開後のβテストFBを見てから優先順位を決定する。

---

### Issue #200: Google Mapsナビゲーション連携を実装する

**背景**: 施設カードに「Google Mapsで開く」ボタンを追加し、現在地から対象施設へのナビゲーションをワンタップで起動できるようにする（論点9-B）。
現在は未実装。MVP公開後のアップデートとして対応する。

**優先度**: 中 / **工数**: 小

- [x] 🔴 RED: 施設カードの「Google Mapsで開く」ボタン押下で正しいナビ用URLが生成されることを確認するテストを書く
- [x] 🟢 GREEN: 施設の Google Maps ディープリンク（`https://www.google.com/maps/place/?q=place_id:${placeId}`）をカードに追加する
- [x] 🔵 REFACTOR: URL生成ロジックをユーティリティ関数に切り出す

---

### Issue #201: 営業時間を考慮した提案フィルタリングを実装する

**背景**: 提案された施設が到着時に閉まっている問題への対応（論点9-C）。
Places APIの `opening_hours` データを活用して営業中の施設のみを提案するオプション。
βテスト時のユーザーFBを踏まえて実装判断する。

**優先度**: 中 / **工数**: 中

- [x] 🔴 RED: 営業時間フィルタON時に、現在時刻が営業時間外の施設が提案されないことを確認するテストを書く
- [x] 🟢 GREEN: New Places API の `opening_hours.open_now` フィールドを取得し、フィルタリングオプションを提案ロジックに追加する。デフォルトOFF
- [x] 🔵 REFACTOR: 営業時間判定ロジックを整理し、深夜時間帯ユーザー（ナイトウォーカー）への影響を確認する（OpenNow=nilの施設は除外しない設計で対応済み）

---

## マーケティング戦略の実行 — Phase 1完成後

> **目的**: アプリが完成する前からファンを作るための「準備」と「始動」の計画

### 1. ブランド・アイデンティティ (Brand Identity)
SNSアカウントを作る前に、アプリの「顔」と「声」を定義する。

- [x] **アプリアイコンの確定**
  - [x] コンセプト決定（例: 「コンパス」「ドア」「靴」「ドット絵風」など、RPG感や冒険を感じさせるもの）
  - [x] ラフ作成 or 既存 `docs/screen-design/icon.png` のブラッシュアップ
  - [x] SNS用リサイズ（正方形 / 丸形トリミングを確認）
- [x] **ブランドカラー・トーンの定義**
  - [x] メインカラー（アクセントカラー）の決定
    アイコンなどで用いるブランドカラーは#525BBBにする。アプリの背景色はコントラストを維持するため#102222で維持
    アプリ内ではあくまでアクセントカラーとして使用し、全体のトーンは落ち着いた暗めの色味で統一する。
- [x] **SNS用ヘッダー画像作成**
  - [x] X (Twitter) 用: 1500x500px
  - [x] YouTube 用: 2560x1440px (TV表示対応エリア注意)

### 2. SNSアカウント・環境セットアップ (Account Setup)
「開発者」ではなく「チャレンジャー」としての舞台を整える。

- [x] **専用Googleアカウント取得** (Gmail)
  - [x] `roamble.official@gmail.com`を取得
- [x] **アカウント開設 & ID統一** (例: `@roamble_app`)
  - [x] **TikTok** (@roamble: https://www.tiktok.com/@roamble)
  - [x] **Instagram** (@Roamble_official: https://www.instagram.com/roamble_official/)
  - [x] **YouTube** (@Roamble: https://www.youtube.com/@Roamble)
  - [x] **X** (@roamble_app: https://x.com/roamble_app)
- [x] **プロフィール文 (Bio) の設定**
  - [x] **共通**: 「コンフォートゾーンを抜け出して新しい場所に辿り着くためのアプリ、「Roamble」の公式アカウント ベータテスター募集中！ 応募はプロフのリンクから👇」
  - [x] **ストーリー**: 「知らない場所に行くのが苦手なエンジニアが、自作アプリに背中を押してもらって歩く記録。#BuildInPublic」
  - [x] **リンク**: LP (Notion) へのリンク設置

### 3. コンテンツ素材の準備 (Content Prep)
動画編集のテンプレートを作り、量産体制を整える。

- [x] **「Day 1」動画の構成案作成**：docs/marketing/Day1_video_script.md にまとめた
- [x] **撮影機材・環境の確認**： スマホで撮ってiMovieで編集する
- [x] **動画テンプレート作成**： 共通のオープニング/エンディング（短く）(docs/marketing/marketing-script.mdやRoamble-CTA.PNGを用いる)
- [x] **必要素材の撮影**： 動画構成案に基づいて、必要な実写素材やスクリーン録画を撮る
  - [x] 提案画面のスクリーン録画（店名と距離をマスクする前提で撮る）
  - [x] 道中の風景（足元、空、街路樹など）
  - [x] 料理や乾杯の手元のクローズアップ
  - [x] アプリの訪問完了画面のスクリーン録画（店名を隠す前提で撮る）
- [x] **編集・公開**(https://x.com/roamble_app/status/2029157147200897235?s=20)

### 4. Phase 1 開発連動アクション (Dev & Marketing Sync)
開発作業そのものをコンテンツにするためのタスク。

- [~] **毎日の開発ログ**: X (Twitter) / Instagram / Tiktok で日々の開発内容や気づきを共有する

### 5. 初動アクション (Launch Actions): Phase 1仕上げ完了後すぐに実行
準備ができ次第、順次実行。

- [x] **LP (Notion) の微修正**
  - [x] 新しいアイコンに差し替え
  - [x] SNSリンクを追加
- [x] **動画 #1「Day 1: 宣言」の投稿**
  - [x] TikTok, Shorts, Reels に同時投稿
  - [x] X で動画のリンクと共に「Roambleで提案された場所に行ってみた」という内容で投稿
- [ ] **Product Hunt / 開発系コミュニティへの「予告」** (Optional)
  - [ ] "Upcoming" ページ等の活用検討
