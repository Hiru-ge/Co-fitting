# TODO — Phase 1 仕上げフェーズ

> Phase 1 の実装はすべて完了。ここからは**自分で実際に使いながら気になった箇所を手直ししていく仕上げフェーズ**に入る。
> バグ・UX改善・細部のブラッシュアップを行い、本番公開に向けた品質を高める。

---

## 🔧 Phase 1 仕上げ（ドッグフーディング）

実際にアプリを使って気になった点をIssue化し、TDDサイクル（🔴RED→🟢GREEN→🔵REFACTOR）で修正する。

---

### Issue #205: バックエンド品質改善（Important）

**背景**: コードレビューで検出された品質改善 7件。Phase 1 仕上げフェーズで対応。

**改善内容**:
1. **`filterOutVisited` のパフォーマンス改善** (`suggestion.go` L529): 全訪問履歴ロード → `WHERE place_id IN (...)` に変更
2. **`CheckAndAwardBadges` のN+1クエリ削減** (`gamification.go` L191): 一括クエリで集計値を取得するか、統計情報をキャッシュ
3. **`weekStart` のタイムゾーン統一** (`gamification.go` L146): UTC → JST に変更し、他のJST判定関数と統一
4. **エラーメッセージの内部情報露出防止** (`suggestion.go` L321, `visit.go` L89): `ShouldBindJSON` エラーを汎用メッセージに変更
5. **訪問座標のバリデーション追加** (`visit.go` L87): lat [-90,90], lng [-180,180] の範囲チェック追加
6. **Redis SCAN+DELETE パターンの共通化**: `database` パッケージに `deleteKeysByPattern` ユーティリティを作成
7. **グローバル変数 `database.DB` の整理**: 将来的にDIのみに統一する方針を決定（段階的移行）

**優先度**: 中 / **工数**: 中

- [ ] 🔴 RED: 各改善項目に対応するテストを書く（特にパフォーマンス改善は既存テストのパス確認）
- [ ] 🟢 GREEN: 上記7件を順次修正する
- [ ] 🔵 REFACTOR: 変更後の一貫性確認、既存テスト全パスを確認する

---

### Issue #206: フロントエンド品質改善（Important）

**背景**: コードレビューで検出された品質改善 8件。Phase 1 仕上げフェーズで対応。

**改善内容**:
1. **`home.tsx` のcustom hook分離**: `useSuggestions` hookを新設し、状態管理ロジックを分離（約280行 → hook + 表示コンポーネント）
2. **コンフェッティ装飾の共通化**: `xp-modal.tsx` / `badge-modal.tsx` から `ConfettiDecoration` コンポーネントを抽出
3. **`profile.tsx` の `<a href>` → `<Link>` 変更**: SPA内遷移でフルリロードが発生する問題を修正（L176-191, L231-241）
4. **モーダルのキーボード操作対応**: 全モーダルに `Escape` キーと背景クリックによるクローズを追加（共通hook `useModalClose` を新設）
5. **`useCallback` 依存配列の修正**: `history.tsx`, `profile.tsx` で `showToast` を依存配列に追加
6. **テストの重複import修正**: `discovery-card.test.tsx` の重複 `DiscoveryCard` importを削除
7. **空ファイル `protected-route.tsx` の削除**: 未使用ファイルを除去
8. **ダークモード対応の統一**: 主要コンポーネントのハードコード明色クラスを `dark:` バリアントで対応

**優先度**: 中 / **工数**: 中

- [ ] 🔴 RED: hook分離後も既存機能が動作すること、モーダルのEscキー操作が機能することを確認するテストを書く
- [ ] 🟢 GREEN: 上記8件を順次修正する
- [ ] 🔵 REFACTOR: コンポーネント構造の整理、既存テスト全パスを確認する

---

### Issue #207: コード品質改善（Suggestion — Phase 2以降）

**背景**: コードレビューで検出された改善提案 10件。Phase 2以降で順次対応。

**改善内容**:
1. API全体のレート制限ミドルウェア追加
2. ヘルスチェックにDB/Redis接続確認を追加
3. `dev_handler.go` の `TestLogin` に追加安全策（IPフィルタ等）
4. テストヘルパー関数を `testutil` パッケージに集約
5. `SuggestionHandler.Suggest` メソッドの機能別分割（約200行）
6. `history.tsx` の写真読み込みバッチ処理・遅延読み込み
7. `level.ts`, `helpers.ts`, `badge-icon.ts` のユニットテスト追加
8. `bottom-nav.tsx`, `app-header.tsx` のテスト追加
9. `settings.tsx` のフォームメッセージ管理を共通hookに統合
10. `getSuggestion` (deprecated) の削除

**優先度**: 低 / **工数**: 中

---

## �🔮 Phase 2 計画（MVP公開後・ドッグフーディングFB次第）

> 仕様見直し（spec-review-agenda.md 論点9）の議論結果を受けたPhase 2タスク群。
> MVP公開後のβテストFBを見てから優先順位を決定する。

---

### Issue #200: Google Mapsナビゲーション連携を実装する

**背景**: 施設カードに「Google Mapsで開く」ボタンを追加し、現在地から対象施設へのナビゲーションをワンタップで起動できるようにする（論点9-B）。
現在は未実装。MVP公開後のアップデートとして対応する。

**優先度**: 中 / **工数**: 小

- [ ] 🔴 RED: 施設カードの「Google Mapsで開く」ボタン押下で正しいナビ用URLが生成されることを確認するテストを書く
- [ ] 🟢 GREEN: 施設の lat/lng を使った Google Maps ディープリンク（`https://www.google.com/maps/dir/?api=1&destination={lat},{lng}`）をカードに追加する
- [ ] 🔵 REFACTOR: URL生成ロジックをユーティリティ関数に切り出す

---

### Issue #201: 営業時間を考慮した提案フィルタリングを実装する

**背景**: 提案された施設が到着時に閉まっている問題への対応（論点9-C）。
Places APIの `opening_hours` データを活用して営業中の施設のみを提案するオプション。
βテスト時のユーザーFBを踏まえて実装判断する。

**優先度**: 中 / **工数**: 中

- [ ] 🔴 RED: 営業時間フィルタON時に、現在時刻が営業時間外の施設が提案されないことを確認するテストを書く
- [ ] 🟢 GREEN: New Places API の `opening_hours.open_now` フィールドを取得し、フィルタリングオプションを提案ロジックに追加する。デフォルトOFF
- [ ] 🔵 REFACTOR: 営業時間判定ロジックを整理し、深夜時間帯ユーザー（ナイトウォーカー）への影響を確認する

---

## マーケティング戦略の実行 — Phase 1完成後

> **目的**: アプリが完成する前からファンを作るための「準備」と「始動」の計画

### 1. ブランド・アイデンティティ (Brand Identity)
SNSアカウントを作る前に、アプリの「顔」と「声」を定義する。

- [x] **アプリアイコンの確定**
  - [x] コンセプト決定（例: 「コンパス」「ドア」「靴」「ドット絵風」など、RPG感や冒険を感じさせるもの）
  - [x] ラフ作成 or 既存 `docs/screen-design/icon.png` のブラッシュアップ
  - [x] SNS用リサイズ（正方形 / 丸形トリミングを確認）
- [x] **ブランドカラー・トーンの定義**
  - [x] メインカラー（アクセントカラー）の決定
    アイコンなどで用いるブランドカラーは#525BBBにする。アプリの背景色はコントラストを維持するため#102222で維持
    アプリ内ではあくまでアクセントカラーとして使用し、全体のトーンは落ち着いた暗めの色味で統一する。
- [x] **SNS用ヘッダー画像作成**
  - [x] X (Twitter) 用: 1500x500px
  - [x] YouTube 用: 2560x1440px (TV表示対応エリア注意)

### 2. SNSアカウント・環境セットアップ (Account Setup)
「開発者」ではなく「チャレンジャー」としての舞台を整える。

- [x] **専用Googleアカウント取得** (Gmail)
  - [x] `roamble.official@gmail.com`を取得
- [x] **アカウント開設 & ID統一** (例: `@roamble_app`)
  - [x] **TikTok** (@roamble: https://www.tiktok.com/@roamble)
  - [x] **Instagram** (@Roamble_official: https://www.instagram.com/roamble_official/)
  - [x] **YouTube** (@Roamble: https://www.youtube.com/@Roamble)
  - [x] **X** (@roamble_app: https://x.com/roamble_app)
- [x] **プロフィール文 (Bio) の設定**
  - [x] **共通**: 「コンフォートゾーンを抜け出して新しい場所に辿り着くためのアプリ、「Roamble」の公式アカウント ベータテスター募集中！ 応募はプロフのリンクから👇」
  - [x] **ストーリー**: 「コミュ障エンジニアが、自作アプリの命令で知らない店に入る記録」というナラティブを入れる
  - [x] **リンク**: LP (Notion) へのリンク設置

### 3. コンテンツ素材の準備 (Content Prep)
動画編集のテンプレートを作り、量産体制を整える。

- [ ] **「Day 1」動画の構成案作成**
  - [ ] スクリプト作成（「今日からこのアプリで人生変えます」宣言）
  - [ ] 絵コンテ（自撮り、PC画面、外の風景のカット割り）
- [ ] **撮影機材・環境の確認**
  - [ ] スマホでの撮影テスト（POV視点、マイク音質チェック）
  - [ ] 画面収録ソフトの準備（PC画面の操作ログ用）
- [ ] **動画テンプレート作成**
  - [ ] 共通のオープニング/エンディング（短く）
  - [ ] テロップのスタイル（ブランドカラーに合わせる）
  - [ ] BGM/SE選定（RPG風のレベルアップ音などを探しておく）

### 4. Phase 1 開発連動アクション (Dev & Marketing Sync)
開発作業そのものをコンテンツにするためのタスク。

- [ ] **「ゲーミフィケーション実装」の予告**
  - [ ] データベース設計図（ER図）をあえて印刷して写真に撮る（「ここが冒険の地図になる」的演出）
  - [ ] レベルテーブルの.md画面をスクショする（「レベル99までの道のりを作った」）
- [ ] **Before状態の記録**
  - [ ] 現在の「味気ない記録画面」を動画に撮っておく（後のAfter動画で劇的変化を見せるため）

### 5. 初動アクション (Launch Actions): Phase 1仕上げ完了後すぐに実行
準備ができ次第、順次実行。

- [x] **LP (Notion) の微修正**
  - [x] 新しいアイコンに差し替え
  - [x] SNSリンクを追加
- [ ] **動画 #1「Day 1: 宣言」の投稿**
  - [ ] TikTok, Shorts, Reels に同時投稿
  - [ ] X で動画のリンクと共に「人生変える実験始めます」とポスト
- [ ] **Product Hunt / 開発系コミュニティへの「予告」** (Optional)
  - [ ] "Upcoming" ページ等の活用検討
